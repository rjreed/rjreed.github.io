(function(){
  function menuSlider(){
    const classes = {
      dropdown: 'navbar_menu_dropdown',
      toggle: 'navbar_menu_toggle'
    };
    
    const elements = {
      dropdown: document.getElementsByClassNames(classes.dropdown)[0],
      toggle: document.getElementsByClassNames(classes.toggle)[0],
    };
    
    
  }
  
})();